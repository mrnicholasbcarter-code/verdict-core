# Audit findings — #257 (CONTEXT) + #258 (CONT-001)

## #257 CONTEXT-001 — publish unchanged + ONE seam (authority)
- 8/9 SATISFIED (provenance, freshness, source attribution, protected-context, compression
  drop-recording, failover rebind, verified/unverified hive quarantine, versioning). Partial: authority.
- SEAM: ContextItem (context_envelope.py:135-185) has `confidence` only, no authority/trust.
  Downstream MemoryRecord already uses trust/authority (hive_workspace.py:274,317). Add optional
  authority enum (default unclassified), include in from_dict/to_dict + digest. ~15 lines.
- If deferred: schema bump 1→2 later (only future break).

## #258 CONT-001 — UNSAFE for durable effects (CONFLICT) → ORF seam
- 5/6 SATISFIED (durable state, checkpoint each step, resume, model/provider replacement +
  context rebind, replay/history). Req #6 (side-effect/idempotency) NOT present.
- CONFLICT: `fail_step` marks step `failed` (execution_session.py:344); `resume_from_failure`
  re-marks SAME step `pending` to re-run (:371-374). If a durable effect (commit/push/PR/merge/
  deploy/memory-promotion) committed but process died before completion checkpoint, resume re-executes → duplicate durable effect.
- ORM SEAM (no tx engine): add `side_effect_kind: Literal["read-only","idempotent","reversible","irreversible"]="read-only"` and `committed: bool=False` to StepRecord; in complete_step checkpoint committed effect immediately; resume_from_failure + complete_step assert committed→skip/raise. Defaults preserve serialization/tests.
